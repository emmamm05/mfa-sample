<h1>Two-Factor Verification</h1>
<p>Please enter the 6-digit code from your authenticator app. If you can't access your app, you may enter one of your backup codes instead.</p>

<%= form_with url: two_factor_verify_path, local: true do |f| %>
  <div>
    <%= label_tag :code, "Authentication or backup code" %><br>
    <%= text_field_tag :code, nil, required: true, autofocus: true, autocomplete: "one-time-code" %>
  </div>
  <div>
    <%= f.submit "Verify" %>
  </div>
<% end %>

<hr>
<div data-controller="webauthn">
  <button data-action="webauthn#authenticate">Use security key</button>
  <div data-webauthn-target="status" style="margin-top: 0.5rem; color: #555;"></div>
</div>

<p>
  <%= link_to "Back to Login", login_path %>
</p>
